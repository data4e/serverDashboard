syntax = "proto3";
package order;
message OrderRequest {
    string phone = 1;
    string price = 2;
    map<string, string> request_arg = 3;
}

message JSONResponse{
    string rst_string = 1;
}

service ServerInfoHandle {
    rpc create_order (OrderRequest) returns (JSONResponse) {}
}
